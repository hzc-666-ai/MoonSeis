# MoonSeis 项目申报书

## 项目基本信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | MoonSeis |
| 项目仓库 | https://github.com/hzc-666-ai/MoonSeis.git |
| 申报方向 | MoonBit 生态基础库 / 科学数据处理工具 / 命令行工具 |
| 开发语言 | MoonBit，少量 native C stub 用于 CLI 文件读取 |
| 开源协议 | Apache-2.0 |
| 申报账号 | hzc-666-ai |

## 项目简介

MoonSeis 是一个使用 MoonBit 编写的 miniSEED 3 波形分析工具包，面向地震波形数据解析、质量检查、信号预处理和事件候选检测等场景。项目提供 miniSEED 3 记录解析、FDSN Source Identifier 解析、Trace 重建、质量评分、STA/LTA 检测，以及 JSON、CSV、GeoCSV、Markdown 和终端文本等多格式输出能力。

项目希望在 MoonBit 生态中补齐一个可复用、可测试、可命令行运行的科学数据处理基础库，为 MoonBit 在地球物理、传感器数据和时间序列分析方向的应用提供示范。

## 背景与意义

miniSEED 是地震波形数据交换和存储中常见的格式，相关工具主要集中在 C、Python、Java 等生态。MoonBit 生态目前仍缺少面向真实科学数据格式的解析与分析项目。MoonSeis 通过实现 miniSEED 3 解析、波形重建和基础分析流程，展示 MoonBit 在二进制格式处理、错误建模、CLI 工具和工程化测试方面的能力。

项目价值主要包括：

- 丰富 MoonBit 科学计算和数据处理生态；
- 提供一个真实格式驱动的 MoonBit 工程化案例；
- 为科研教学、台站数据检查和波形预处理提供轻量工具；
- 为后续 Steim 解压、miniSEED 2.4、Wasm 可视化等能力预留扩展空间。

## 核心功能

1. **miniSEED 3 解析**：解析固定头、SID、extra headers、payload，并支持 CRC-32C 校验。
2. **样本解码**：支持 Int16、Int32、Float32、Float64、Text 和 Opaque 等数据类型。
3. **Trace 重建**：按 SID 分组并按时间排序，检测 gap、overlap、duplicate 和采样率变化。
4. **质量检查**：内置 clipping、constant signal、spike 等规则，并生成 0–100 分质量评分。
5. **信号处理**：支持 demean、detrend、normalize、Hann taper、RMS、统计量和基础重采样。
6. **事件候选检测**：实现 Classic / Recursive STA/LTA，并输出事件区间与置信度。
7. **CLI 输出**：支持 inspect、records、validate、traces、quality、detect、convert 等命令。

## 技术方案

项目采用多包分层架构：

```text
Bytes / .mseed
  -> miniseed3: 固定头与 CRC
  -> record: SID、JSON、时间和 payload
  -> encoding: 样本解码
  -> trace: 分组、排序和分段诊断
  -> quality / signal / trigger: 质量检查、信号处理和事件检测
  -> formats: 多格式输出
  -> cmd/main: native CLI
```

主要包包括 `binary`、`time`、`sid`、`miniseed3`、`encoding`、`record`、`trace`、`quality`、`signal`、`trigger`、`formats` 和 `cmd/main`。核心库不依赖文件系统，CLI 只作为薄封装，便于未来适配 Wasm 或其他宿主环境。

## 当前完成情况

目前项目已经完成基础可运行版本：

- 已完成 MoonBit 多包项目结构；
- 已实现 miniSEED 3 基础解析、SID 解析、时间模型和 CRC 校验；
- 已实现样本解码、Trace 构建、质量检查、信号处理和 STA/LTA 检测；
- 已实现 native CLI 和 `fixtures/example.mseed` 端到端示例；
- 已提供 README、LICENSE、架构文档和 GitHub Actions CI 配置；
- 本地已通过 `moon check`、`moon test`、`moon fmt` 和 CLI smoke test。

## 实施计划

1. **基础解析阶段**：完成字节读写、miniSEED 3 固定头、CRC、SID 和时间模型。
2. **波形分析阶段**：完成 record 迭代、Trace 重建、分段诊断、质量规则和信号处理。
3. **CLI 阶段**：完成 STA/LTA 检测、多命令 CLI 和多格式输出。
4. **完善阶段**：补充 README、架构文档、测试 fixture、CI 和开源发布材料。
5. **后续扩展**：增加 Steim 解压、miniSEED 2.4、真实数据回归测试和 Wasm 可视化示例。

## 预期交付物

- 公开 GitHub 仓库：`https://github.com/hzc-666-ai/MoonSeis.git`；
- 可通过 `moon check` 和 `moon test` 验证的 MoonBit 项目；
- 可运行的 native CLI 工具；
- 可复现的 miniSEED fixture 和测试用例；
- README、架构说明、申报书、许可证和 CI 配置。

## 使用示例

```powershell
moon check
moon test
moon run cmd/main -- inspect fixtures/example.mseed
moon run cmd/main -- validate fixtures/example.mseed
moon run cmd/main -- quality fixtures/example.mseed
moon run cmd/main -- detect fixtures/example.mseed
moon run cmd/main -- convert fixtures/example.mseed csv
```

## 开源合规

项目采用 Apache-2.0 许可证，代码以原创 MoonBit 实现为主。示例 fixture 由项目内部生成器构造，用于测试和 CLI 演示。若后续引入外部数据、第三方代码或生成代码，将在文档中明确来源、许可证和再分发约束。

## 项目总结

MoonSeis 以 miniSEED 3 波形数据处理为切入点，展示 MoonBit 在二进制解析、科学数据处理、命令行工具和工程化测试方面的能力。项目具有明确的实用场景和可扩展方向，适合作为 MoonBit 国产开源生态中的基础软件与科研工具项目持续推进。
