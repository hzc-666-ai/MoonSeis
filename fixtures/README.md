# Fixtures

- `example.mseed`: 3 条合法 CRC 的 miniSEED 3 Int16 记录。
- 两条记录属于 `FDSN:IU_ANMO_00_B_H_Z`（20 Hz），一条属于
  `FDSN:IU_NNA_00_B_H_Z`（10 Hz）。
- 文件由与 `internal/fixtures/fixtures.mbt` 相同的布局生成，用于 CLI smoke test。
